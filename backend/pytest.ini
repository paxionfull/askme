[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    network: 需要访问外网的测试（discovery_validate 等）
    slow: 全量 discovery 契约测试（本地或 nightly 运行）
filterwarnings =
    ignore::DeprecationWarning
